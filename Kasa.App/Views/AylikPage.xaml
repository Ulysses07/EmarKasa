<?xml version="1.0" encoding="UTF-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Kasa.App.Views.AylikPage" Title="Aylık">
    <VerticalStackLayout Spacing="12">
        <Grid ColumnDefinitions="Auto,Auto" ColumnSpacing="8">
            <Label Text="{Binding Yil}" FontSize="20" />
            <Label Grid.Column="1" Text="{Binding Ay}" FontSize="20" />
        </Grid>
        <CollectionView ItemsSource="{Binding Rapor.Kanallar}">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Grid Padding="8" ColumnDefinitions="*,Auto">
                        <Label Text="{Binding Kanal}" />
                        <Label Grid.Column="1" Style="{StaticResource Para}" Text="{Binding AySonucu, Converter={StaticResource ParaBicim}}" />
                    </Grid>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </VerticalStackLayout>
</ContentPage>
