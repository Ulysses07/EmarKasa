<?xml version="1.0" encoding="UTF-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Kasa.App.Views.PanelPage" Title="Panel">
    <ScrollView>
        <VerticalStackLayout Spacing="12">
            <Border Style="{StaticResource Kart}">
                <VerticalStackLayout>
                    <Label Text="Güncel Kasa" TextColor="{StaticResource Sub}" />
                    <Label Style="{StaticResource Para}" FontSize="28" Text="{Binding GuncelKasa, Converter={StaticResource ParaBicim}}" />
                </VerticalStackLayout>
            </Border>
            <CollectionView ItemsSource="{Binding Kanallar}">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Grid Padding="8" ColumnDefinitions="*,Auto">
                            <Label Text="{Binding Kanal}" />
                            <Label Grid.Column="1" Style="{StaticResource Para}" Text="{Binding Bakiye, Converter={StaticResource ParaBicim}}" />
                        </Grid>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
